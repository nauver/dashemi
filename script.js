const q=(s,c=document)=>c.querySelector(s),qa=(s,c=document)=>[...c.querySelectorAll(s)];const observer=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');observer.unobserve(e.target)}}),{threshold:.12});qa('.reveal').forEach(e=>observer.observe(e));const counter=q('.counter');const countObserver=new IntersectionObserver(es=>es.forEach(e=>{if(!e.isIntersecting)return;let n=0,t=+counter.dataset.target;const tick=()=>{n+=1;counter.textContent=n;if(n<t)requestAnimationFrame(tick)};tick();countObserver.disconnect()}));countObserver.observe(counter);const menu=q('.menu'),nav=q('#nav');menu.addEventListener('click',()=>{const open=nav.classList.toggle('open');menu.setAttribute('aria-expanded',open)});qa('#nav a').forEach(a=>a.addEventListener('click',()=>{nav.classList.remove('open');menu.setAttribute('aria-expanded','false')}));const theme=q('#theme');theme.addEventListener('click',()=>{document.body.classList.toggle('darkmode');theme.setAttribute('aria-label',document.body.classList.contains('darkmode')?'Activer le thème clair':'Activer le thème sombre')});const modal=q('#summary'),openBtn=q('#summaryBtn');function closeModal(){modal.hidden=true;openBtn.focus()}function openModal(){modal.hidden=false;q('.close',modal).focus()}openBtn.addEventListener('click',openModal);q('.close',modal).addEventListener('click',closeModal);q('.close-link',modal).addEventListener('click',closeModal);modal.addEventListener('click',e=>{if(e.target===modal)closeModal()});document.addEventListener('keydown',e=>{if(e.key==='Escape'&&!modal.hidden)closeModal()});q('#print').addEventListener('click',()=>window.print());